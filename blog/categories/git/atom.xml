<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Category: git | polina on rails]]></title>
  <link href="http://psoshnin.github.io/blog/categories/git/atom.xml" rel="self"/>
  <link href="http://psoshnin.github.io/"/>
  <updated>2014-05-21T21:51:26-04:00</updated>
  <id>http://psoshnin.github.io/</id>
  <author>
    <name><![CDATA[Polina Soshnin]]></name>
    
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[git workflow and branching]]></title>
    <link href="http://psoshnin.github.io/blog/2014/02/25/polina-on-rails-gitworkflow-and-branching/"/>
    <updated>2014-02-25T21:35:22-05:00</updated>
    <id>http://psoshnin.github.io/blog/2014/02/25/polina-on-rails-gitworkflow-and-branching</id>
    <content type="html"><![CDATA[<p><a href="http://infinitemonkeys.influitive.com/a-simple-explanation-for-git-rebase/">http://infinitemonkeys.influitive.com/a-simple-explanation-for-git-rebase/</a></p>

<p>Version control is the bread and butter of open source and professional software development and yet I found it almost absent in my academic computer science education. Version control is a way to track changes to your software projects and management collaboration. For those completely new to it though, it can be really confusing to first download the Mac GUI client for github and then realize it&rsquo;s pretty pointless for learning how to actually use and understand how a successful git branching model works. I will run through the basic commands you need to be successful with git and then describe how I do feature branching in my projects.</p>

<h2>Basic git</h2>

<p>Assuming you&rsquo;ve played around with git before, these are my usual go-to commands:</p>

<p>Configure git
```</p>

<pre><code>$ git -v
$ git config --global user.name "Your Name"
$ git config --global user.email youremail@example.com
$ git config --global user.username githubusername //configure for github
</code></pre>

<p>```</p>

<p>Committing changes to git
```</p>

<pre><code>$ git init . //only do this once to initialize git repository
$ git add -A
$ git commit -m "initial commit"
</code></pre>

<p>```</p>

<p>Creating a feature branch and then merging it into the main branch
```</p>

<pre><code>$ git checkout -b feature-branch //creates and checks it out at the same time
$ git add -A
$ git commit -m "add feature changes"
$ git checkout master
$ git merge --squash feature-branch //squashes your commits
$ git commit -m "add feature commits"
$ git branch -D feature-branch //delete working branch
</code></pre>

<p><code>
Checking in on a project
</code></p>

<pre><code>$ git status //check status of changes
$ git diff //view changes to files
</code></pre>

<p>```</p>

<p>Connecting a local reposity to an empty remote you just created on github and pushing changes to it
```</p>

<pre><code>$ git remote add origin &lt;URLFROMGITHUB&gt;
$ git push origin master
$ git remote add &lt;REMOTENAME&gt; &lt;URL&gt; //add additional remote connections
$ git pull &lt;REMOTENAME&gt; &lt;BRANCHNAME&gt; //pulling in changes
$ git remote -v //view remote connections
$ git push &lt;REMOTENAME&gt; &lt;BRANCH&gt; //push to remote connections
</code></pre>

<p>```</p>

<p>Forking and cloning projects
```</p>

<pre><code>$ git clone &lt;URL&gt; //fork a project on github and then clone it to work on it locally
</code></pre>

<p>```</p>

<p>If the original repository you forked changes, you want to be able to pull in those changes, so you add a remote connection to the original. Most people name the remote connection &lsquo;upstream&rsquo;.</p>

<p>```</p>

<pre><code>$ git remote add upstream https://github.com/username/forkedproject.git
</code></pre>

<p>```</p>

<p>Add collaborators&mdash; it makes software fun! You can add collaborators directly to your project via your github account.</p>

<p>Working with collaborators
```</p>

<pre><code>$ git status 
$ git fetch --dry-run //see changes to remote before you pull in
$ git pull &lt;REMOTENAME&gt; &lt;REMOTEBRANCH&gt; //pull in changes from remote branch
</code></pre>

<p>```</p>

<p>Creating a pull request
1. Visit original repository you forked on github,  <a href="http://www.github.com/username/projectname">http://www.github.com/username/projectname</a>
2. Click &ldquo;Pull request&rdquo; on the right-side menu, then &ldquo;New pull request&rdquo;
3. Select branch with the changes you want to submit
4. The page should then show the changes associated with your pull request.</p>

<p>Merging a branch</p>

<p>```</p>

<pre><code>$ git checkout &lt;BRANCHNAME&gt;
$ git merge &lt;BRANCHNAME&gt;
$ git branch -D &lt;BRANCHNAME&gt;
$ git push &lt;REMOTENAME&gt; -- delete &lt;BRANCHNAME&gt; //you can also delete the branch from your fork on github
</code></pre>

<p>```</p>
]]></content>
  </entry>
  
</feed>
